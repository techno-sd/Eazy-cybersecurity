# ==============================================
# Eazy Cyber - Environment Variables Template
# ==============================================
#
# INSTRUCTIONS:
# 1. For LOCAL development: Copy this file to .env.local and fill in your values
# 2. For PRODUCTION: Set these via your hosting platform (Vercel, etc.)
#
# SECURITY WARNING:
# - NEVER commit .env.local, .env, or .env.production files with real values
# - Always use environment variables from your hosting platform for production
# - Generate strong secrets using: openssl rand -base64 32
# ==============================================

# ==============================================
# DATABASE CONFIGURATION (Required)
# ==============================================
#
# Full connection string for Prisma to connect to MySQL database
# Format: mysql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME?ssl-mode=REQUIRED
#
DATABASE_URL="mysql://your_db_user:your_db_password@your_db_host:3306/your_db_name?ssl-mode=REQUIRED"

# Individual database connection parameters
DB_HOST=your_db_host
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
DB_SSL=true

# ==============================================
# AUTHENTICATION SECRETS (Required)
# ==============================================
#
# JWT Secret - Generate with: openssl rand -base64 32
# MUST be at least 32 characters for security
JWT_SECRET=your_jwt_secret_min_32_characters_long

# Encryption Key - Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your_encryption_key_min_32_characters

# ==============================================
# SITE CONFIGURATION (Required for Production)
# ==============================================
#
# Your website URL (used in emails, redirects, CORS)
# Development: http://localhost:3000
# Production: https://your-domain.com
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==============================================
# ADMIN CONFIGURATION (Change in Production!)
# ==============================================
#
# Initial admin credentials - CHANGE THESE IMMEDIATELY
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_secure_password

# ==============================================
# ANALYTICS (Optional)
# ==============================================
#
# Google Analytics Measurement ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Tag Manager ID
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# ==============================================
# EMAIL CONFIGURATION (Optional)
# ==============================================
#
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=noreply@your-domain.com

# ==============================================
# FILE UPLOAD (Optional)
# ==============================================
#
# MAX_FILE_SIZE=5242880
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif
